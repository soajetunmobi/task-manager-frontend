import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#005070',
    },
    secondary: {
      main: '#49C6DD',
    },
    error: {
      main: '#F24C62',
    },
  },
});

export default theme;
